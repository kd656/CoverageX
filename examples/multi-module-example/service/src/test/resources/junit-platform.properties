# Enables JUnit 5's auto-registration of ServiceLoader-declared extensions,
# which is how the CoverageX test listener (coveragex-test-junit5) attaches
# itself. Without this, the extension is on the classpath but never activates
# and the HTML report shows "Test tracking requires the CoverageX test listener."
junit.jupiter.extensions.autodetection.enabled=true
